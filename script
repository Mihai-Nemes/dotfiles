#!/bin/bash

# This script links your dotfiles to the home directory in Codespaces
echo "Configuring environment..."

# Get the directory of this script
DOTFILES_DIR=$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)

# Link .bashrc to the home directory if it exists
if [ -f "$DOTFILES_DIR/.bashrc" ]; then
    cat "$DOTFILES_DIR/.bashrc" >> ~/.bashrc
fi

# You can add more automated installations here, like personal CLI tools:
# sudo apt-get update && sudo apt-get install -y htop
